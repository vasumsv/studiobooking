spring:
  datasource:
    url: jdbc:postgresql://studiobooking.postgres.database.azure.com:5432/studiobookingdb?sslmode=require
    username: vasumsv
    password: "NewLife3!"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect